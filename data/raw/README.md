# Raw data

Bulk source downloads are stored here during execution and are intentionally ignored by Git. Their URLs, release identifiers, retrieval timestamps, sizes, and checksums belong in `data/manifests/` and the final run manifest.

